using System;

/// <summary>
/// A delegate allows one or more classes to get an event that's fired
/// from a sender class
///
/// Common in WPA
/// </summary>
public delegate void CardEventHandler(object sender, CardEventArgs e);

